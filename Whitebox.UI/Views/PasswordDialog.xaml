<Window x:Class="Whitebox.UI.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Whitebox.UI.Views"
        mc:Ignorable="d"
        Title="PasswordDialog" Height="204.545" Width="362.338">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Grid.Row="0" Grid.ColumnSpan="2" />
        <TextBox Text="{Binding Password}" HorizontalAlignment="Stretch" Height="22"  Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Center" Margin="5,2" />
        <Button Click="Button_Click" Content="Button" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Center" Width="70" Grid.Column="1"/>
    </Grid>
</Window>
